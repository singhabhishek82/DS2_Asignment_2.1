# Program which accepts a sequence of comma-separated numbers from console and generate a list.
inputval = input("Enter the numbers seperated by a comma :")
list = inputval.split(',')
print("Output as List :",list)